{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}

{{- $regular := ( where .Site.RegularPages.ByPublishDate.Reverse "Section" "post" )  }}
{{- $napkin := where .Site.RegularPages.ByPublishDate.Reverse "Section" "napkin" }}
{{- $pages := union $regular $napkin }}

{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ .Site.Title }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content at {{ .Site.Title }}</description>
    {{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Author.email }}
    <managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Author.email }}
    <webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{ with .OutputFormats.Get "RSS" }}
	{{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{ end }}
    {{ range sort $pages "PublishDate" "desc" }}
    {{ if (not (isset .Params "hidden")) }}
    <item>
      <title>{{ .Title }}</title>
      {{ if .Params.external_url }}
        <link>{{ .Params.external_url }}</link>
      {{ else }}
        <link>{{ .Permalink }}</link>
      {{ end }}

      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}
      <guid>{{ .Permalink }}</guid>
      {{ if .Params.external_url }}
        <description>This content was posted on another site. <a href="{{ .Params.external_url }}">Read or watch it on the other site.</a></description>
      {{ else }}
        <description>{{ .Summary | html }}</description>
      {{ end }}
    </item>
    {{ end }}
    {{ end }}
  </channel>
</rss>
